<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>New question</title>
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="shortcut icon" href="img/img3.png" type="image/png">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Open+Sans:400,600%7CRoboto:300,400,500" media="all">
        <link href="login.ejs"stylesheet">
        <link href="/css/icomoon.css" rel="stylesheet">
        <link href="/css/font-awesome.min copy.css" rel="stylesheet">
        <link href="/css/main.css" rel="stylesheet">
        <link href="/css/queries.css" rel="stylesheet">

    </head>
<%- include('templates/header', {titulo: "Pagina Usuario Pro"}) %>
    <body>
        
        <%- include('templates/menu') %>

        
            <div class="container mt-5">
                <form action="/newpag2" method="POST">
                    <% for (contact of contacts) { %>
                        <% if(contact.UserId == user.id) {%>
                        <p>oferta: <%=contact.nameContact%></p>
                        <p>email: <%=contact.emailContact%></p>
                        <p>phone: <%=contact.phone%></p>
                        <p>direction: <%=contact.direction%></p>
                        <li>
                            <a href="/delete/<%=contact.id %>"> eliminar </a>
                            <a href="/edit/<%=contact.id %>">editar</a>
                        </li>
                        <% } %>
                    <% } %>
            
                            <a class="btn btn-primary" href="/" role="button">retorno</a>

                </form>
            </div> 
    <script type="text/javascript" src="js/main.js"></script>
    <script type="text/javascript" src="js/jquery-3.5.1.min.js"></script>
    <script src="js/moment-with-locales.min.js"></script>
    <script type="text/javascript" src="/socket.io/socket.io.js"></script>

    </body>
</html>
    

    